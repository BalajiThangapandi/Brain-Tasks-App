version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20

  pre_build:
    commands:
      - echo Logging into DockerHub

  build:
    commands:
      - echo Build started
      - docker build -t brain-tasks-app .

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'

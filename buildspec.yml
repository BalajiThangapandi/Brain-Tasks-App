version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR

  build:
    commands:
      - echo Build started
      - docker build -t brain-tasks-app .

  post_build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - '**/*'
